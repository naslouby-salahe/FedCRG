# FedCRG outputs root — generated artifacts live here and are not committed.
# Directory scaffolding is preserved via .gitkeep so the layout contract test
# can verify the reserved structure in a clean checkout.

# Re-allow the scaffolding marker files
!**/.gitkeep

# Everything else under outputs/ is generated at runtime
*
