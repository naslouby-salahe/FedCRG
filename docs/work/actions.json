{
  "as_of": "2026-08-14T12:00:00Z",
  "actions": [
    {"id": "A001", "milestone": "m1", "description": "runner.py: replace hand-rolled topological sort with graphlib.TopologicalSorter; materialize ORACLE_TEST via oracle_choice on final-test evidence; fix build_run_id detector label; remove unused imports", "status": "pending"},
    {"id": "A002", "milestone": "m1", "description": "evidence/store.py: enforce RunManifestStore immutability for completed/failed runs; remove unused imports and the detector_label dead variable", "status": "pending"},
    {"id": "A003", "milestone": "m1", "description": "data/preprocessing.py: strengthen _reuse_existing to validate source/role file existence and hashes against the manifest; add constant_features to PreprocessingModel; fix external_replication_supported; implement locked N-BaIoT/DIAD attack-development allocation in split_base", "status": "pending"},
    {"id": "A004", "milestone": "m1", "description": "learning/federated.py: add model_communication/preprocessing_communication/threshold_policy_communication deterministic ledgers (roadmap 8.3, 14.5.1); remove unused imports and dead variables", "status": "pending"},
    {"id": "A005", "milestone": "m2", "description": "cli.py: rewrite to target surface (validate, preprocess, plan, run, campaign, status, monitor, report, results build/verify, doctor) wired to real modules; fix pyproject entry point to fedcrg.cli:cli", "status": "pending"},
    {"id": "A006", "milestone": "m3", "description": "Rewrite tests/_fixtures.py on fedcrg.config; migrate all 38 broken test files to target module layout preserving scientific assertions; delete tests for removed behavior", "status": "pending"},
    {"id": "A007", "milestone": "m4", "description": "Add contract tests: preprocessing reuse (10 scenarios), model/score cache reuse invalidation, results bundle build/verify/tamper, runtime console observability", "status": "pending"},
    {"id": "A008", "milestone": "m5", "description": "Rewrite tools/audit_repository.py for target architecture; align Makefile, noxfile, README, CI with the final CLI and quality gate", "status": "pending"},
    {"id": "A009", "milestone": "m6", "description": "Strengthen ruff select and pyright strict; run ruff format+check, pyright, full pytest, nox -s quality, CLI smoke (validate/plan/preprocess/status/monitor/results help); iterate hostile audit until convergence", "status": "pending"}
  ]
}
