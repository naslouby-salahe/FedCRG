id: primary_nbaiot
protocol: ../../protocol/default.yaml
dataset: ../../datasets/nbaiot.yaml
detector: ../../detectors/autoencoder_nbaiot.yaml
training:
  rounds: 30
  local_epochs: 120
  batch_size: 64
  optimizer: adam
  learning_rate_initial: 0.001
  learning_rate_final: 0.00001
  adam_betas: [0.9, 0.999]
  adam_epsilon: 0.00000001
  weight_decay: 0.0
  client_fraction: 1.0
  aggregation: equal_client_mean
  mixed_precision: false
  device: cpu
randomness:
  model_seeds: [11, 22, 33, 44, 55]
  attack_split_seed: 9001
  synthetic_seed: 123456
  bootstrap_seed: 424242
policies:
  - reference_quantile
  - global_quantile
  - local_quantile
  - readiness_only
  - mismatch_only
  - shrinkage
  - three_sigma
  - dev_f1_select
  - summary_statistic_select
  - supervised_f1
  - oracle_test
  - fedcrg
outputs_root: outputs
