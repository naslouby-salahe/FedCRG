# Development Tools

This directory is reserved for optional developer utilities that are not imported by `fedcrg`, required by the CLI, or part of the scientific execution path.

Scientific-contract checks belong in `tests/contract/`; behavioral unit tests belong in `tests/unit/`; end-to-end flows belong in `tests/integration/`. Do not recreate the removed catch-all `scripts/` directory.
