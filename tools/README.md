# Development Tools

This directory is reserved for optional developer utilities that are not imported by `fedcrg`, required by the CLI, or part of the scientific execution path.

Scientific-contract checks belong in `tests/contract/`; regressions belong in `tests/regression/`. Do not recreate the removed catch-all `scripts/` directory.
